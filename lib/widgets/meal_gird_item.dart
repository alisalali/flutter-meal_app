import 'package:flutter/material.dart';

class MealGirdItem extends StatelessWidget {
  const MealGirdItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
